# Tahini Sidecar - Bazel workspace
# Requires SGX_SDK in the environment (e.g. /opt/intel/sgxsdk) for SGX targets.
# Dependencies are managed via Bzlmod (MODULE.bazel); Bazel 8 uses Bzlmod by default.

workspace(name = "tahini_sidecar")
